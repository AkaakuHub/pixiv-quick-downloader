pre-commit:
  parallel: true
  jobs:
    - name: type check
      run: pnpm type-check
      glob: "*.{ts,tsx}"

    - name: lint
      run: pnpm lint
      glob: "*.{ts,tsx}"

    - name: format check
      run: pnpm format:check
      glob: "*.{ts,tsx,html,json}"
