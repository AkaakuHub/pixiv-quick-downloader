pre-commit:
  parallel: true
  jobs:
    - name: type check
      run: pnpm type-check
      glob: "*.{ts,tsx}"

    - name: format
      run: pnpm format
      glob: "*.{ts,tsx,html,json}"

    - name: lint
      run: pnpm lint
      glob: "*.{ts,tsx}"

    - name: knip check
      run: pnpm knip
